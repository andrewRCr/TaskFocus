@inherits LayoutComponentBase

<MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="_theme" />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>

    <MudAppBar Elevation="0">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        <MudSpacer />

        <AuthorizeView>
            <Authorized>
                <MudLink Href="Account/Manage">@context.User.Identity?.Name</MudLink>
                <MudIcon Icon="@Icons.Material.Filled.Logout" Color="Color.Inherit" Class="ml-6 mr-3"/>
                <MudLink Href="logout">Log Out</MudLink>
            </Authorized>
            <NotAuthorized>
                <Login />
            </NotAuthorized>
        </AuthorizeView>
    </MudAppBar>


    <MudDrawer @bind-Open="@_drawerOpen" Elevation="1">
        <MudDrawerHeader>
            <MudText Typo="Typo.h6">TaskFocus</MudText>
        </MudDrawerHeader>

        <NavMenu />

    </MudDrawer>

    <MudMainContent>
        @Body
    </MudMainContent>

</MudLayout>

@code {
    protected bool _drawerOpen = true;
    private MudTheme _theme = new();
    private bool _isDarkMode = true;

    protected void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
}