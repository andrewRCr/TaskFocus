<UserControl x:Class="TaskFocusDesktop.Views.StatusInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewmodels="clr-namespace:TaskFocusDesktop.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:StatusInfoViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <StackPanel Orientation="Vertical">
            <TextBlock x:Name="Header" FontSize="18" FontWeight="Bold" Margin="10" />
            <TextBlock x:Name="Message" FontSize="16" Margin="10" />
            <Button x:Name="Close" Padding="10" Margin="20">Close</Button>
        </StackPanel>
            
    </Grid>
</UserControl>
