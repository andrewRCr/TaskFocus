<UserControl x:Class="TaskFocusDesktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskFocusDesktop.Views"
             mc:Ignorable="d" Background="White" FontSize="18" FontFamily="Bahnschrift"
             d:DesignHeight="425" d:DesignWidth="800">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="0 20 0 40"
                   HorizontalAlignment="Center" FontSize="32" FontFamily="Bahnschrift">
            Account Login
        </TextBlock>
        
        <!-- Error message row -->
        <TextBlock x:Name="ErrorMessage" 
                   Visibility="{Binding IsErrorMsgVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"
                   Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0 0 0 5"
                   MaxWidth="346" TextWrapping="Wrap" FontSize="14" Foreground="Red">
        </TextBlock>

        <!-- Username row -->
        <TextBlock Grid.Row="2" Grid.Column="1" Margin="0 0 0 10">
            Username:
        </TextBlock>
        <TextBox x:Name="Username" Grid.Row="2" Grid.Column="2" MinWidth="250"
                 Margin="10 0 0 10" Padding="2" FontSize="14" />

        <!-- Password row -->
        <TextBlock Grid.Row="3" Grid.Column="1" Margin="0 0 0 10">
            Password:
        </TextBlock>
        <PasswordBox x:Name="Password" Grid.Row="3" Grid.Column="2" MinWidth="250" 
                     Margin="10 0 0 10" Padding="2" FontSize="14" />
        
        <!-- Login row -->
        <Button Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="0 15 0 0" Padding="20 5"
                HorizontalAlignment="Center" x:Name="Login">
            Log In
        </Button>
        
    </Grid>
</UserControl>
